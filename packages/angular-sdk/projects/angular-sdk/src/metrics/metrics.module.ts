import { NgModule } from "@angular/core";
import { SCSDKMetricsService } from "./services/metrics.service";

@NgModule({
    providers: [
        SCSDKMetricsService
    ]
})
export class SCSDKMetricsModule {}