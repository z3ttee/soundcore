{
  "name": "soundcore",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "config/*",
    "packages/*"
  ],
  "scripts": {
    "ci:build": "turbo run ci:build --no-daemon"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "^3.5.3",
    "turbo": "2.4.4"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "yarn@1.22.19"
}